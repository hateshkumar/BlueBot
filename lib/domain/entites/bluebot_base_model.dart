abstract class BlueBotBaseModel {
  /// For now keys should just be String really
  String get key => throw UnimplementedError();

  /// TODO implement all over
  Map<String, dynamic> toJson() {
    throw UnimplementedError();
  }
}
